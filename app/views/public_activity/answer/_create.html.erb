<li>
  <span>
    <a href="<%= question_path(a.trackable.question.id) %>"><%= a.trackable.question.title %></a>
    <%= t "activity.is_answered" %>
    <% if a.owner.present? %>
      <b class="owner_name"><%= a.owner.name %></b>
    <% end %>
  </span>
</li>
