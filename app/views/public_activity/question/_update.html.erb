<li>
  <span>
    <a href="<%= question_path(a.trackable.id) %>"><%= a.trackable.title %></a>
    <%= t "activity.update" %>
    <% if a.owner.present? %>
      <b class="owner_name"><%= a.owner.name %></b>
    <% end %>
  </span>
</li>
